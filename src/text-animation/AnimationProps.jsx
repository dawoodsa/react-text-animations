import React from 'react'
import { useEffect } from 'react/cjs/react.development'

export default function setAnimationProperties({animationProps}) {

    useEffect(()=>{
        setTimeout(() => {
            document.querySelector('')
        }, delay);
    })

    return <></>
}
